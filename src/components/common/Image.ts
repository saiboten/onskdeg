import styled from 'styled-components';

export const ImageWrapper = styled.div`
      max-width: 36px;
      max-height: 36px;
      overflow: hidden;
      position: absolute;
      left: 8px;
      top: 8px;
    `;